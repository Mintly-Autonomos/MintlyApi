# Copie pra .env.e2e e preencha com o URI do Atlas.
# E2E usa o header env=e2e pra rotear pro database "e2e" no cluster.
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>/
PORT=3001
API_URL=http://localhost:3001
